DentalCare React + Bootstrap

Run:
npm install
npm run dev

Backend API:
cd backend-api
npm install
npm start

The backend runs at http://localhost:8080. Open that address to confirm it is running.

The pages are React JSX components. The old HTML pages are not needed.
Bootstrap is installed as a dependency and loaded from src/main.jsx.
